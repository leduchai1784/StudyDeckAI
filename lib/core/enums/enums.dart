enum QuestionType { multipleChoice, fillInBlank, matching, speaking }
enum ActivityType { flashcard, quiz, reading, listening, writing, speaking }
enum RecommendationType { review, newLesson, weakPointFix }
enum UserRole { student, teacher, admin }
